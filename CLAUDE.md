# CLAUDE.md

Follow `AGENTS.md`.

Read order: `AGENTS.md` -> `SPEC.md` -> `ARCHITECTURE.md` ->
`docs/deferred-work.md` when relevant -> `docs/features/<feature>.md`.

Gate before finishing: `make check`.
