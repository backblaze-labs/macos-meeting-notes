[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "macos-meeting-notes"
version = "0.1.0"
description = "Local-first macOS app for Meeting Memory notes, transcription, and B2 backup."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Meeting Memory contributors" }]
keywords = [
  "ai-notes",
  "backblaze-b2",
  "google-calendar",
  "local-first",
  "macos",
  "meeting-notes",
  "meeting-transcripts",
  "menu-bar-app",
  "speaker-diarization",
  "transcription",
]
dependencies = [
  "anthropic>=0.40",
  "assemblyai>=0.35",
  "boto3>=1.35",
  "google-api-python-client>=2.150",
  "google-auth-oauthlib>=1.2",
  "keyring>=25.0",
  "pydantic>=2.9",
  "pydantic-settings>=2.6",
  "python-dotenv>=1.0",
  "rumps>=0.4",
]

[project.optional-dependencies]
dev = [
  "pre-commit>=4.0",
  "pytest>=8.3",
  "ruff>=0.8",
]

[project.scripts]
meeting-memory = "meeting_memory.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"meeting_memory.service.assets" = ["*.icns"]
"meeting_memory.ui.assets" = ["*.png", "*.svg"]
"meeting_memory.repo" = ["native/*.swift"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "T20", "UP"]

[tool.ruff.lint.per-file-ignores]
"src/meeting_memory/doctor.py" = ["UP036"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
